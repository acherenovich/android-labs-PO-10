<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Lab6" parent="Theme.MaterialComponents.DayNight.NoActionBar"> <!-- Используем NoActionBar -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Стиль для красных кнопок -->
    <style name="RedButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/coca_cola_red</item>
        <item name="android:textColor">@color/white</item>
        <!-- Можно добавить другие атрибуты, например, corner radius -->
        <item name="cornerRadius">8dp</item>
    </style>
</resources>